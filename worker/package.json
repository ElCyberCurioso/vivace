{
  "name": "guitarchords-sync-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker fronting an R2 bucket for GuitarChords song sync",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check src/api.js && node --check src/sync.js && node check-pages.mjs",
    "test": "node test-detect.mjs && node --test test/"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
